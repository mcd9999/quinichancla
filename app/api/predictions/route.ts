import { NextResponse } from "next/server"; import { db } from "@/lib/db"; import { session } from "@/lib/auth";
export async function POST(req:Request){const s=await session();if(!s)return NextResponse.json({error:"No autorizado"},{status:401});const form=await req.formData();const poolId=Number(form.get("poolId"));const conn=await db().getConnection();try{await conn.beginTransaction();const [pools]=await conn.query<any[]>("SELECT stake FROM pools WHERE id=? AND status='OPEN' AND deadline>NOW() FOR UPDATE",[poolId]);if(!pools[0])throw new Error("Quiniela cerrada");const [matches]=await conn.query<any[]>("SELECT id FROM matches WHERE pool_id=?",[poolId]);const picks=matches.map(m=>({id:m.id,value:String(form.get(`match_${m.id}`))}));if(!picks.length||picks.some(p=>!["1","X","2"].includes(p.value)))throw new Error("Pronóstico incompleto");const [[bal]]=await conn.query<any[]>("SELECT balance FROM user_balances WHERE user_id=?",[s.userId]);if(Number(bal?.balance||0)<Number(pools[0].stake))throw new Error("Saldo insuficiente");const [result]=await conn.query<any>("INSERT INTO predictions(user_id,pool_id) VALUES(?,?)",[s.userId,poolId]);for(const p of picks)await conn.query("INSERT INTO prediction_items(prediction_id,match_id,forecast) VALUES(?,?,?)",[result.insertId,p.id,p.value]);await conn.query("INSERT INTO ledger(user_id,pool_id,type,amount,note,created_by) VALUES(?,?,'STAKE',?,'Participación en quiniela',?)",[s.userId,poolId,-Number(pools[0].stake),s.userId]);await conn.commit();return NextResponse.redirect(new URL("/pronosticos?ok=1",req.url),303)}catch(e){await conn.rollback();return NextResponse.json({error:e instanceof Error?e.message:"Error"},{status:400})}finally{conn.release()}}

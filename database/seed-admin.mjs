import mysql from "mysql2/promise"; import bcrypt from "bcryptjs";
const [,,name,email,password]=process.argv;if(!name||!email||!password){console.error("Uso: node database/seed-admin.mjs Nombre email contraseña");process.exit(1)}if(!process.env.DATABASE_URL)throw new Error("DATABASE_URL no configurada");const db=await mysql.createConnection(process.env.DATABASE_URL);const hash=await bcrypt.hash(password,12);await db.execute("INSERT INTO users(name,email,password_hash,role) VALUES(?,?,?,'ADMIN')",[name,email.toLowerCase(),hash]);await db.end();console.log("Administrador creado");
